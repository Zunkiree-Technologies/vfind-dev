import React, { useState } from 'react';
import { Search, MapPin, Mail, DollarSign, Clock, Clock1, Briefcase } from 'lucide-react';

const IndependentScrollDemo = () => {
  const [search, setSearch] = useState('');
  const [location, setLocation] = useState('');

  // Sample data for demonstration
  const candidates = Array.from({ length: 15 }, (_, i) => ({
    id: i + 1,
    fullName: `Candidate ${i + 1}`,
    qualification: 'Registered Nurse',
    location: 'Sydney, NSW',
    email: `candidate${i + 1}@example.com`,
    payRate: 45 + i * 2,
    experience: `${2 + i} years`,
    jobType: 'Full-Time',
    shift: 'Morning'
  }));

  const filterOptions = Array.from({ length: 20 }, (_, i) => `Filter Option ${i + 1}`);

  return (
    <div className="min-h-screen bg-[#F5F6FA]">
      {/* Navbar - sticky */}
      <div className="sticky top-0 z-50 bg-white shadow-sm">
        <div className="h-16 flex items-center px-6">
          <h1 className="text-xl font-bold text-gray-800">Employer Dashboard</h1>
        </div>
      </div>

      {/* Search Bar - sticky below navbar */}
      <div className="sticky top-16 z-40 bg-[#F5F6FA] py-3 shadow-sm">
        <div className="flex justify-center">
          <div className="flex items-center w-[95%] max-w-[950px] border border-gray-300 rounded-lg bg-white overflow-hidden p-1">
            <div className="flex items-center px-3 w-1/2">
              <Search className="text-gray-400 mr-2" size={18} />
              <input
                value={search}
                onChange={(e) => setSearch(e.target.value)}
                type="text"
                placeholder="Search by keyword, designation, company name"
                className="w-full outline-none py-2"
              />
            </div>
            <div className="w-px h-6 bg-gray-300" />
            <div className="flex items-center px-3 w-1/3">
              <MapPin className="text-gray-400 mr-2" size={18} />
              <input
                value={location}
                onChange={(e) => setLocation(e.target.value)}
                type="text"
                placeholder="City, State or ZIP code"
                className="w-full outline-none py-2"
              />
            </div>
            <button className="px-6 py-2 bg-blue-400 text-white font-medium hover:bg-blue-500 rounded-lg transition-colors">
              Search
            </button>
          </div>
        </div>
      </div>

      {/* Main Container - Fixed height with independent scroll areas */}
      <div className="container mx-auto px-4 py-6">
        <div className="flex gap-6" style={{ height: 'calc(100vh - 180px)' }}>
          
          {/* Left Filters - Independent scrollable area */}
          <div className="hidden md:block w-[320px] flex-shrink-0">
            <div className="h-full overflow-y-auto bg-white rounded-lg p-4 shadow-sm">
              <div className="space-y-6">
                <div className="flex justify-between items-center sticky top-0 bg-white pb-3 -mt-1 pt-1 z-10">
                  <h2 className="font-semibold text-gray-800">All Filters</h2>
                  <button className="text-sm text-blue-600 hover:text-blue-700">
                    Clear All
                  </button>
                </div>
                <div className="h-px bg-gray-300" />

                {/* Filter sections */}
                <div>
                  <h3 className="font-medium text-sm text-gray-700 mb-3">Job Type</h3>
                  {["Full-Time", "Part-Time", "Casual", "Open to any"].map((type) => (
                    <div key={type} className="flex items-center gap-2 text-sm mb-2">
                      <input type="checkbox" className="rounded" />
                      <label>{type}</label>
                    </div>
                  ))}
                </div>
                <div className="h-px bg-gray-300" />

                <div>
                  <h3 className="font-medium text-sm text-gray-700 mb-3">Shift</h3>
                  {["Morning", "Afternoon", "Night"].map((shift) => (
                    <div key={shift} className="flex items-center gap-2 text-sm mb-2">
                      <input type="checkbox" className="rounded" />
                      <label>{shift}</label>
                    </div>
                  ))}
                </div>
                <div className="h-px bg-gray-300" />

                <div>
                  <h3 className="font-medium text-sm text-gray-700 mb-3">Role Category</h3>
                  {[
                    "Clinical Lead / Manager",
                    "Registered Nurse (RN)",
                    "Enrolled Nurse (EN)",
                    "Assistant in Nursing (AIN)",
                    "Personal Care Assistant (PCA)",
                    "Attendant",
                    "Support Worker",
                    "Nursing Assistant",
                  ].map((role) => (
                    <div key={role} className="flex items-center gap-2 text-sm mb-2">
                      <input type="checkbox" className="rounded" />
                      <label>{role}</label>
                    </div>
                  ))}
                </div>
                <div className="h-px bg-gray-300" />

                {/* Extra filters for scrolling demo */}
                <div>
                  <h3 className="font-medium text-sm text-gray-700 mb-3">Additional Filters</h3>
                  {filterOptions.map((option) => (
                    <div key={option} className="flex items-center gap-2 text-sm mb-2">
                      <input type="checkbox" className="rounded" />
                      <label>{option}</label>
                    </div>
                  ))}
                </div>
              </div>
            </div>
          </div>

          {/* Right Candidate List - Independent scrollable area */}
          <div className="flex-1 overflow-y-auto">
            <div className="space-y-4 pr-2">
              {candidates.map((candidate) => (
                <div
                  key={candidate.id}
                  className="flex justify-between items-center bg-white rounded-lg p-6 shadow-sm hover:shadow-md transition-shadow"
                >
                  <div className="flex-1">
                    <h2 className="font-semibold text-lg text-[#61A6FA] mb-1">
                      {candidate.fullName}
                    </h2>
                    <p className="text-sm text-gray-600 mb-3">{candidate.qualification}</p>

                    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 text-gray-600 text-sm">
                      <div className="flex items-center gap-2">
                        <MapPin size={16} />
                        <span>{candidate.location}</span>
                      </div>
                      <div className="flex items-center gap-2">
                        <Mail size={16} />
                        <span>{candidate.email}</span>
                      </div>
                      <div className="flex items-center gap-2">
                        <DollarSign size={16} />
                        <span>${candidate.payRate}/hr</span>
                      </div>
                      <div className="flex items-center gap-2">
                        <Clock1 size={16} />
                        <span>{candidate.experience}</span>
                      </div>
                      <div className="flex items-center gap-2">
                        <Briefcase size={16} />
                        <span>{candidate.jobType}</span>
                      </div>
                      <div className="flex items-center gap-2">
                        <Clock size={16} />
                        <span>{candidate.shift}</span>
                      </div>
                    </div>
                  </div>

                  <div className="flex items-center gap-4 ml-6 flex-col">
                    <div className="h-16 w-16 rounded-full overflow-hidden border flex-shrink-0 bg-gray-200 flex items-center justify-center">
                      <span className="text-xs text-gray-500">No Photo</span>
                    </div>
                    <button className="px-4 py-2 bg-[#61A6FA] text-white text-sm font-medium rounded-lg hover:bg-blue-500 transition-colors">
                      View Details
                    </button>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default IndependentScrollDemo;